version: 2.1

jobs:
  new:
    steps:
      - run:
          name: verificacion js
          command: 
            pwd
            ls
            apt install node-acorn 

  
  

workflows:
  version: 2.1
  deploy:
    jobs:
      - new:
          filters:
            branches:
              only: main
      

