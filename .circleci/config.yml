version: 2.1
jobs:
  build:
    machine: 
      enable: true
    steps:
      - add_ssh_key: # check out the code in the project directory
          fingerprints:
             - "cf:97:96:8d:49:45:d9:91:ec:a2:c6:cc:32:8f:91:e9"
      
workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build:
          filters:
            branches:
              only: main

